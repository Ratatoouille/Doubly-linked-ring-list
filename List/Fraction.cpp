#include "Fraction.h"

template<typename T>
Fraction<T>::Fraction(T numerator, T denominator)
{
	this->numerator = numerator;
	this->denominator = denominator;
}

template<typename T>
void Fraction<T>::print()
{
}

template<typename T>
void Fraction<T>::read()
{
}
